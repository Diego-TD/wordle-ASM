# Wordle on Assembly + C
Final project for the Operative Systems class
- C project that handles the input and output
- ASM files that process the main algorithm

## Run the project
Let's see if we can leverage CMakeLists.txt to run the project, if not manually build the exec on a /build folder.
